apiVersion: v1
kind: Service
metadata:
  name: ELB-SVC
  namespace: {{ .Release.Namespace }}
  labels:
    app: ELB-SVC
spec:
  type: LoadBalancer
  selector:
    app: foodopia-app
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP